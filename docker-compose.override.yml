# O PostgreSQL de serving roda fora do stack Astro. Processos do host usam
# POSTGRES_HOST=localhost; o scheduler usa o gateway Docker para alcançá-lo.
services:
  scheduler:
    environment:
      - POSTGRES_HOST=host.docker.internal
    extra_hosts:
      - host.docker.internal:host-gateway
