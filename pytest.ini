[pytest]
# Escopo padrão do `pytest` local (sem Airflow instalado no .venv).
# `astro dev pytest` roda dentro do container e passa "tests" explicitamente,
# cobrindo também tests/dags/.
testpaths = tests/collectors
pythonpath = .
